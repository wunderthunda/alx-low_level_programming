#include "main.h"
#include <stdio.h>

/**
 * _isupper - Check for upper case letter
 *
 * @c : string to check
 *
 * return: 0
 */
int 0-isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
		return (0);
}
